# GitHub Repository Settings

Apply these values in the repository **About** settings:

- Description: `Angular portfolio hub that unifies tested TypeScript mini projects with reusable UI, themes, i18n, and GitHub Pages deployment.`
- Website: `https://angjelspasovski.github.io/projects-hub/`
- Topics: `angular`, `typescript`, `javascript`, `portfolio`, `frontend`, `primeng`, `bootstrap`, `scss`, `mini-projects`

The connected repository account has admin access, but the available automation connector does not expose repository metadata updates and no authenticated GitHub CLI is installed locally. Keep the matching task open until GitHub confirms the saved values.
