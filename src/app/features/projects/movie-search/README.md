# Movie Search

Standalone Angular movie search project with a local API-style dataset.

## Scope

- Search by movie title, director, or cast member.
- Filter by genre.
- Paginate results.
- Show selected movie details.
- Cover loading, empty, retry, and error states without requiring an API key.

The component is structured so a real OMDb or TMDB integration can replace the local dataset later.
