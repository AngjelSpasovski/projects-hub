# REST Countries

Standalone Angular project that models a REST Countries API experience with local data.

## Scope

- Search countries by name, capital, or code.
- Filter by region.
- View selected country details.
- Persist favorite countries in LocalStorage.
- Cover loading, empty, retry, and error states without requiring network access.

The state model is ready to replace the local country list with a real REST Countries API client later.
